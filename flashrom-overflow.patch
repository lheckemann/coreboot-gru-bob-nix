diff --git a/file.c b/file.c
index 7b39428b..015635dc 100644
--- a/file.c
+++ b/file.c
@@ -207,7 +207,7 @@ static int do_fdt_find_spi_nor_flash(const char *prefix,
 	 */
 	while (!found && (d = readdir(dp))) {
 		char node[PATH_MAX];
-		char pattern[PATH_MAX];
+		char pattern[PATH_MAX+16];
 		char alias[64];
 		int i, fd, len;
 		glob_t pglob;
